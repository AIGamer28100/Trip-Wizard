name: trip-wizards-backend
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - poetry
  - nodejs # For any frontend tooling if needed
  - pip:
      - fastapi==0.104.1
      - uvicorn[standard]==0.24.0
      - pydantic==2.5.0
      - firebase-admin==6.2.0
      - httpx==0.25.2
      - python-multipart==0.0.6
      - websockets==12.0
      # Dev dependencies
      - pytest==7.4.3
      - pytest-asyncio==0.21.1
      - ruff==0.1.6
      - black==23.11.0
